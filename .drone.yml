kind: pipeline
type: docker
name: default

environment:
  GOOS: linux
  GOARCH: amd64

steps:
- name: build
  image: ghcr.io/hugomods/hugo:latest
  commands:
  - cd website
  - hugo --minify --gc --enableGitInfo --debug
